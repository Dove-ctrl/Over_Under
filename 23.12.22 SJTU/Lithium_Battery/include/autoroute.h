#ifndef AUTOROUTE_H_
#define AUTOROUTE_H_

#include "vex.h"
#include "autofunction.h"
using namespace vex;

void PlanA(competition*);

void _auto_arm(void*);
void _auto_take(void*);
void _auto_piston(void*);
void _auto_shoot(void*);

#endif